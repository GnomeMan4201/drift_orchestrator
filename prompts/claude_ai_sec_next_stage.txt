You are entering an existing local-first engineering system mid-flight.

STRICT ENGINEERING MODE ENABLED.

Your job is to extend an already-working control system accurately and robustly. Do not simplify. Do not summarize. Do not restate this prompt. Do not give high-level advice. Do not use placeholders. Do not produce partial examples. Do not produce pseudocode.

You must respond ONLY with full command blocks I can paste into my terminal.

Every file update must be delivered as complete, copy-paste-safe shell commands using this exact style:

cat > /absolute/or/project/relative/path <<'EOF2'
...full file contents...
EOF2

If modifying an existing file, return the FULL updated file, not a diff unless I explicitly ask for a diff.

If a file should be patched instead of fully rewritten, use a safe Python patcher block:
python3 - <<'PY'
from pathlib import Path
...
PY

If a directory must exist, create it explicitly with mkdir -p.

If permissions must change, include chmod.

If a command should be run after writing files, include it.

Do not wrap command blocks in explanation.
Do not include prose between command blocks.
Do not explain what you are doing.
Do not ask questions.
Do not omit imports.
Do not use placeholders like TODO, SNIP, your_path_here, or similar.
Do not rely on external dependencies unless already present in Python standard library.
Assume Python 3.12.
Assume Linux shell environment.
Assume local-first workflow.
Assume the repo is drift_orchestrator.
Assume robustness matters more than brevity.

## SYSTEM CONTEXT

We have built a local-first AI execution control system centered on a command called `ai-sec`.

This system is a closed-loop evaluator + repair engine for LLM-generated code.

### Current pipeline
1. Generate code through `ai`
2. Extract first Python block
3. Compile check with py_compile
4. Run rule-based semantic validation
5. Score output 0-100
6. If degraded, run multi-pass repair loop
7. Re-score each repair pass
8. Select best candidate
9. Emit telemetry
10. Compute drift score alpha
11. Log to JSONL
12. Visualize in dashboard
13. Feed signal into drift_orchestrator

### Current working components
- ~/bin/ai-sec
- ~/bin/ai-sec-log
- ~/bin/ai-sec-alpha
- ~/bin/ai-sec-stats
- ~/research_hub/repos/drift_orchestrator/runtime_signals/dashboard_api.py
- ~/research_hub/repos/drift_orchestrator/ai_sec_bridge.py

### Current working capabilities
- syntax validation
- semantic rule checks
- AST checks
- weighted scoring
- multi-pass repair
- best-pass selection
- alpha drift computation
- JSONL telemetry
- dashboard
- bridge into drift_orchestrator

### Current critical limitation
The system can still produce:
- score = 100
- issues = []

while the code is still functionally wrong.

This is the missing layer:
OPERATIONAL CORRECTNESS DRIFT

Examples of failures we still need to catch:
- valid APIs used incorrectly
- output shape mismatches
- list/dict schema mismatches
- future/result misuse
- incorrect host/port mapping
- incorrect iteration over CIDR outputs
- logically invalid but syntactically valid code
- runtime-invalid transformations that pass static checks

## WHAT WE WANT TO BUILD NEXT

We want to extend the system with a robust workflow for:

1. EXECUTION CORRECTNESS VALIDATION
   - detect correct-looking but logically wrong code
   - add deterministic checks, not vague heuristics

2. SAFE RUNTIME VALIDATION
   - lightweight execution probes only
   - no real network activity
   - no unsafe actions
   - validate structure, data shape, and function behavior safely

3. DATA SHAPE VALIDATION
   - ensure expected schema and result layout
   - detect invalid unpacking and structure mismatches
   - validate JSON-serializable outputs

4. REPAIR QUALITY ANALYSIS
   - did repair preserve task intent
   - did repair introduce new issues
   - did repair structurally degrade code
   - did repair improve or worsen alpha

5. SCORING EXTENSION
   - expand score model to include operational correctness
   - include stronger penalties for runtime-invalid but compile-valid outputs

6. PIPELINE INTEGRATION
   - integrate new modules into ai-sec
   - preserve current working behavior
   - preserve existing telemetry output
   - preserve alpha computation but make it more accurate

## REQUIRED OUTPUT

Respond ONLY with terminal-ready command blocks that do all of the following:

A. Build a new validation layer
Create any needed files and update existing ones so ai-sec gains:
- execution correctness checks
- safe runtime validation
- data shape validation
- repair quality analysis

B. Extend the scoring model
Update scoring so:
- operational correctness issues are weighted
- repair regressions are penalized
- false score-100 passes are much harder

C. Extend telemetry
Ensure JSONL output captures:
- execution correctness issues
- runtime validation results
- repair quality deltas
- richer alpha components

D. Extend dashboard
Update dashboard to show:
- alpha
- stability
- score
- verdict
- issue counts
- repair pass behavior
- optional repair gain / repair degradation signal

E. Keep workflow robust
Your response must implement a robust workflow that:
- creates backups where appropriate
- writes files safely
- avoids fragile heredoc corruption patterns where possible
- includes verification commands at the end

## HARD CONSTRAINTS

- Python standard library only
- no external Python dependencies
- safe local execution only
- no live network scanning during validation
- Python 3.12 compatible
- Linux shell compatible
- full file rewrites preferred over brittle fragments when safer
- if you modify ai-sec, return the full updated file
- if you modify dashboard_api.py, return the full updated file
- if you modify ai_sec_bridge.py, return the full updated file
- if you add new modules, wire them into the workflow
- no prose outside command blocks
- no markdown headings outside command blocks
- no bullet points outside command blocks
- no explanation

## IMPORTANT RESPONSE RULE

Respond only in command blocks that I can paste directly into my terminal to build this accurately with a robust workflow.
